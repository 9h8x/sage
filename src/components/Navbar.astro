---
import { SignInButton, SignedIn, SignedOut, UserButton } from "@clerk/astro/components";
---
<nav class="fixed top-4 left-4 right-4 z-50">
  <div class="navbar card-backdrop rounded-box shadow-lg">
    <div class="navbar-start">
      <div class="dropdown">
        <div tabindex="0" role="button" class="btn btn-ghost lg:hidden">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" />
          </svg>
        </div>
        <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow card-backdrop rounded-box w-52">
          <li><a href="/">Inicio</a></li>
          <SignedIn>
            <li><a href="/dashboard">Panel</a></li>
          </SignedIn>
        </ul>
      </div>
      <a class="btn btn-ghost text-xl">Panel SAGE</a>
    </div>
    <div class="navbar-center hidden lg:flex">
      <ul class="menu menu-horizontal px-1">
        <li><a href="/">Inicio</a></li>
        <SignedIn>
          <li><a href="/dashboard">Panel</a></li>
        </SignedIn>
      </ul>
    </div>
    <div class="navbar-end gap-2">
      <SignedIn>
        <div class="mx-5 mt-2">
          <UserButton afterSignOutUrl="/" />
        </div>
      </SignedIn>
      <SignedOut>
        <SignInButton mode="modal" asChild>
          <button class="btn btn-primary">Iniciar Sesión</button>
        </SignInButton>
      </SignedOut>
    </div>
  </div>
</nav>