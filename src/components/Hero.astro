---
import { SignInButton, SignedIn, SignedOut } from "@clerk/astro/components";
import { Icon } from "astro-icon/components";
---

<div class="min-h-[calc(100vh-4rem)] flex items-center justify-center content-padding py-16">
  <div class="max-w-2xl w-full text-center">
    <div class="space-y-8">
      <div class="flex justify-center">
        <Icon name="mdi:database-search" class="w-20 h-20 text-white/80 animate-float" />
      </div>
      
      <div class="space-y-4">
        <h1 class="text-5xl font-bold tracking-tight animate-slide-up">
          Bienvenido al Panel SAGE
        </h1>
        <p class="text-xl text-white/80 leading-relaxed animate-slide-up">
          Herramienta para obtener información directamente de SAGE de cualquier persona, en cualquier momento.
        </p>
      </div>

      <div class="flex justify-center gap-4 animate-slide-up">
        <SignedIn>
          <a href="/dashboard" class="btn btn-primary px-8 py-3 rounded-full text-lg font-medium">
            <span class="flex items-center gap-2">
              Ir a Panel
              <Icon name="mdi:arrow-right" class="w-5 h-5" />
            </span>
          </a>
        </SignedIn>
        <SignedOut>
          <SignInButton mode="modal">
            <button class="btn btn-primary px-8 py-3 rounded-full text-lg font-medium">
              <span class="flex items-center gap-2">
                Iniciar Sesión
                <Icon name="mdi:login" class="w-5 h-5" />
              </span>
            </button>
          </SignInButton>
        </SignedOut>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  .animate-slide-up {
    opacity: 0;
    animation: slide-up 0.5s ease-out forwards;
  }
</style>